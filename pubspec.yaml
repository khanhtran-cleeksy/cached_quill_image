name: cached_quill_image
description: Flutter library to load and cache network or local images.
  Can also be used with placeholder and error widgets.
version: 0.0.1

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.3.0
  octo_image: ^2.0.0
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.1.1
  flutter_lints: ^1.0.4
  file: ^6.1.0

environment:
  flutter: ">=3.3.0"
  sdk: ">=2.18.0 <3.0.0"
